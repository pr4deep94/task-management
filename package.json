{
  "name": "mern-task-manager",
  "version": "1.0.0",
  "description": "A MERN application for basic tasks management",
  "main": "index.js",
  "scripts": {
    "dev-server": "npm run start:dev --prefix server",
    "dev-client": "npm start --prefix client",
    "dev": "concurrently \"npm run dev-server\" \"npm run dev-client\"",
    "install-dependency": "npm install && npm install --prefix client && npm install --prefix server"
  },
  "devDependencies": {
    "concurrently": "^7.1.0"
  },
  "dependencies": {
    "@nestjs/typeorm": "^10.0.2",
    "mongodb": "^5.9.2",
    "typeorm": "^0.3.20"
  }
}
